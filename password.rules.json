{
  "rules": [
    {
      "name": "length",
      "message": "Password must be a minimun 5 characters",
      "regex": "^.{5,}$"
    },
    {
      "name": "digit",
      "message": "Password must have at least one digit",
      "regex": "\\d"
    },
    {
      "name": "repeating_char",
      "message": "Password must not have more than two repeating characters",
      "regex": "([a-zA-Z0-9])\\1{2,}"
    },
    {
      "name": "upper_case_special_char",
      "message": "Password must have at least one upper-case or one special character",
      "regex": ".*[A-Z].*|[^A-Za-z0-9]"
    }
  ]
}
